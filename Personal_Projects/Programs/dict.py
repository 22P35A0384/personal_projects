d = {} #key value pairs
d.keys()
d.values()
d.items()
d[key] = value
d.update({k1:v2, k2:v2.........})
d.pop(key)
d.popitem() #it deletes the last item in the dict
d.copy() #duplicate dict
d.get(key)
d[key]
d.clear()
d.setdefault(key,value)
dict.fromkeys(kt,v)
del
len
